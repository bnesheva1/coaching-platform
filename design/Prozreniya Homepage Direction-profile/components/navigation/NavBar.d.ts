export interface NavBarProps {
  /** @startingPoint section="Navigation" subtitle="Top nav — wordmark, links, language toggle, auth actions" viewport="1200x90" */
  lang?: 'BG' | 'EN';
  links?: string[];
}
