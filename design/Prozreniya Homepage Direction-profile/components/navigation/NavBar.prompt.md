NavBar — top-level site navigation. Composes `Button` (ghost + primary) for auth actions; language toggle is a plain pill (not a Button).

```jsx
<NavBar lang="BG" links={['За търсещи', 'За практици', 'Как работи']} />
```
